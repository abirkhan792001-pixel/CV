{
  "name": "cv",
  "version": "1.0.0",
  "private": true,
  "description": "One-page CV, tailored for Deloitte Luxembourg financial risk management",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "preview": "node build.mjs --png",
    "share": "node build.mjs && python3 scripts/finalise.py && python3 scripts/audit.py"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
